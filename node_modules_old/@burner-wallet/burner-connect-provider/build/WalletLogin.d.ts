export default class WalletLogin {
    origin: string;
    private iframe;
    private msgId;
    constructor(origin: string);
    createIframe(visible?: boolean): any;
    needsPopup(): Promise<boolean>;
    showPopup(container: any): Promise<void>;
    getFrame(): any;
    remove(): void;
}
