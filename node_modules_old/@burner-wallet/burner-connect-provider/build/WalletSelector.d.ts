import { Wallet } from './BurnerProvider';
import HubBridge from './HubBridge';
export default class WalletSelector {
    private container;
    private panel;
    private closeListeners;
    getPanel(): any;
    showStarting(): void;
    showSelector(bridge: HubBridge, defaultWallets?: Wallet[]): Promise<any>;
    showFrame(iframe: any): void;
    showConnecting(wallet: string): void;
    close(): void;
    onClose(listener: any): void;
}
