export default class HubBridge {
    private iframe;
    private url;
    private msgId;
    constructor(url: string);
    ensureIFrame(): Promise<any>;
    getFrame(defaultWallets?: any[]): any;
    awaitSelection(): Promise<any>;
    send(command: string): Promise<unknown>;
}
