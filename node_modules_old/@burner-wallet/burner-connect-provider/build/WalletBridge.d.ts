export default class HubBridge {
    origin: string;
    private iframe;
    private iframeLoadPromise?;
    private msgId;
    constructor(origin: string);
    send(payload: any): Promise<unknown>;
    ensureIFrame(): Promise<any>;
    getFrame(): any;
    sendCommand(command: string, params?: any): Promise<unknown>;
    remove(): void;
}
